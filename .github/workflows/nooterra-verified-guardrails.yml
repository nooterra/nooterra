name: nooterra-verified-guardrails

on:
  workflow_dispatch: {}
  pull_request:
    paths:
      - "src/**"
      - "scripts/ci/run-nooterra-verified-gate.mjs"
      - "scripts/ci/run-agent-substrate-adversarial-harness.mjs"
      - "test/**"
      - "docs/spec/public/NooterraVerified.v1.md"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/nooterra-verified-guardrails.yml"
  push:
    branches: [main]
    paths:
      - "src/**"
      - "scripts/ci/run-nooterra-verified-gate.mjs"
      - "scripts/ci/run-agent-substrate-adversarial-harness.mjs"
      - "test/**"
      - "docs/spec/public/NooterraVerified.v1.md"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/nooterra-verified-guardrails.yml"

concurrency:
  group: nooterra-verified-guardrails-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      NOOTERRA_CI_ALLOW_BOOTSTRAP_LOCAL: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run Nooterra Verified guardrails gate
        run: npm run -s test:ops:nooterra-verified-gate -- --level guardrails --out artifacts/gates/nooterra-verified-guardrails-gate.json
      - name: Upload guardrails gate artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nooterra-verified-guardrails-${{ github.run_id }}
          path: |
            artifacts/gates/nooterra-verified-guardrails-gate.json
            artifacts/ops/agent-substrate-adversarial-harness.json
          if-no-files-found: warn
