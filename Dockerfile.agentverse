FROM node:20-alpine

WORKDIR /app
ENV NODE_ENV=production

COPY package*.json ./
RUN npm ci --omit=dev && npm cache clean --force

COPY bin ./bin
COPY src ./src

USER 65532:65532

ENTRYPOINT ["node", "bin/agentverse-cli.js"]
CMD ["--help"]
