apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nooterra.fullname" . }}-api
  labels:
    {{- include "nooterra.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "nooterra.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        {{- include "nooterra.labels" . | nindent 8 }}
        app.kubernetes.io/component: api
      annotations:
        {{- toYaml .Values.api.podAnnotations | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.api.podSecurityContext | nindent 8 }}
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "{{ .Values.api.service.port }}"
            - name: STORE
              value: {{ .Values.store.mode | quote }}
            - name: PROXY_PG_SCHEMA
              value: {{ .Values.store.pgSchema | quote }}
            - name: PROXY_MIGRATE_ON_STARTUP
              value: {{ ternary "1" "0" .Values.store.migrateOnStartup | quote }}
            - name: PROXY_EVIDENCE_STORE
              value: {{ .Values.evidenceStore.mode | quote }}
            {{- if eq .Values.evidenceStore.mode "s3" }}
            - name: PROXY_EVIDENCE_S3_ENDPOINT
              value: {{ required "evidenceStore.s3.endpoint is required when evidenceStore.mode=s3" .Values.evidenceStore.s3.endpoint | quote }}
            - name: PROXY_EVIDENCE_S3_REGION
              value: {{ .Values.evidenceStore.s3.region | quote }}
            - name: PROXY_EVIDENCE_S3_BUCKET
              value: {{ required "evidenceStore.s3.bucket is required when evidenceStore.mode=s3" .Values.evidenceStore.s3.bucket | quote }}
            - name: PROXY_EVIDENCE_S3_FORCE_PATH_STYLE
              value: {{ ternary "1" "0" .Values.evidenceStore.s3.forcePathStyle | quote }}
            - name: PROXY_EVIDENCE_S3_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ required "evidenceStore.s3.accessKeyIdSecret.name is required when evidenceStore.mode=s3" .Values.evidenceStore.s3.accessKeyIdSecret.name }}
                  key: {{ .Values.evidenceStore.s3.accessKeyIdSecret.key }}
            - name: PROXY_EVIDENCE_S3_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ required "evidenceStore.s3.secretAccessKeySecret.name is required when evidenceStore.mode=s3" .Values.evidenceStore.s3.secretAccessKeySecret.name }}
                  key: {{ .Values.evidenceStore.s3.secretAccessKeySecret.key }}
            {{- end }}
            {{- if .Values.buildId }}
            - name: PROXY_BUILD
              value: {{ .Values.buildId | quote }}
            {{- end }}
            {{- if .Values.config.exportDestinationsJson }}
            - name: PROXY_EXPORT_DESTINATIONS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "nooterra.fullname" . }}-config
                  key: PROXY_EXPORT_DESTINATIONS
            {{- end }}
            {{- if eq .Values.store.mode "pg" }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ required "store.databaseUrlSecret.name is required when store.mode=pg" .Values.store.databaseUrlSecret.name }}
                  key: {{ .Values.store.databaseUrlSecret.key }}
            {{- end }}
            {{- range $k, $v := .Values.api.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          securityContext:
            {{- toYaml .Values.api.securityContext | nindent 12 }}
          volumeMounts:
            {{- if .Values.api.volumes.tmp.enabled }}
            - name: tmp
              mountPath: /tmp
            {{- end }}
            {{- if .Values.api.volumes.data.enabled }}
            - name: data
              mountPath: {{ .Values.api.volumes.data.mountPath }}
            {{- end }}
            {{- range .Values.extraSecretMounts }}
            - name: {{ .name }}
              mountPath: {{ .mountPath }}
              readOnly: true
            {{- end }}
          readinessProbe:
            httpGet:
              path: {{ .Values.api.readinessProbe.path | quote }}
              port: http
            initialDelaySeconds: {{ .Values.api.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.api.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.api.readinessProbe.failureThreshold }}
            successThreshold: {{ .Values.api.readinessProbe.successThreshold }}
          livenessProbe:
            httpGet:
              path: {{ .Values.api.livenessProbe.path | quote }}
              port: http
            initialDelaySeconds: {{ .Values.api.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.api.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.api.livenessProbe.failureThreshold }}
          startupProbe:
            httpGet:
              path: {{ .Values.api.startupProbe.path | quote }}
              port: http
            initialDelaySeconds: {{ .Values.api.startupProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.startupProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.api.startupProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.api.startupProbe.failureThreshold }}
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
      volumes:
        {{- if .Values.api.volumes.tmp.enabled }}
        - name: tmp
          emptyDir: {}
        {{- end }}
        {{- if .Values.api.volumes.data.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
        {{- range .Values.extraSecretMounts }}
        - name: {{ .name }}
          secret:
            secretName: {{ .secretName }}
            {{- if .items }}
            items:
              {{- toYaml .items | nindent 14 }}
            {{- end }}
        {{- end }}
