# Bundle fixture corpus (v1)

These directories are realistic, end-to-end bundles intended for CLI verification tests.

Regenerate (deterministically) with:

```sh
node scripts/fixtures/generate-bundle-fixtures.mjs
```

The fixture expectations live in `test/fixtures/bundles/v1/fixtures.json`.

